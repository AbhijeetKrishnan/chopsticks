# Pyrefly type-checker configuration.
# See https://pyrefly.org/en/docs/configuration/ for the full option list.

python_version = "3.12"

project_includes = [
    "src",
    "tests",
]

project_excludes = [
    "**/__pycache__",
    ".venv",
    "build",
    "dist",
]
